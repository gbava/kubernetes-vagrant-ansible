apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ app_name }}
  namespace: default
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: {{ app_name }}
    spec:
      containers:
      - name: {{ app_name }}
        image: gbava/spring-boot-sample-hateoas:v1
        env:
        - name: AUTHOR
          value: {{ app_name }}
        ports:
        - containerPort: 8080