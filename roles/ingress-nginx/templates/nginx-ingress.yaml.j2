---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ app_name }}
  namespace: default
spec:
  tls:
    - hosts:
      - {{ app_name }}.{{ fqdn }}
      secretName: tls-certificate
  rules:
  - host: {{ app_name }}.{{ fqdn }}
    http:
      paths:
      - path: /
        backend:
          serviceName: {{ app_name }}
          servicePort: 8080
